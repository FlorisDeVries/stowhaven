apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
    name: cleanup-staging-cron
spec:
    type: bindings.cron
    version: v1
    metadata:
        - name: schedule
          value: "@every 24h"
scopes:
    - backup-api
